version: "2.0"

rules:

- rule: Sempre finalize quando o usuario disser tchau
  steps:
  - intent: despedir
  - action: utter_despedir

- rule: Sempre pergunte o numero do processo
  steps:
  - intent: consultar_processo
  - action: utter_qual_numero_processo

